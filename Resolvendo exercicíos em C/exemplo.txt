#include <stdio.h>
// std = Standard (Padrão)
// io = input/output (Entrada/Saída)

int main(){
	//Declaração de variáveis
	setbuf(stdout, NULL); //limpar a memoria cach
	
	//Entrada
	
	//Processamentos
	
	//Saída
}